"""
Rationale generation.

Will use the LLM to generate evidence-backed rationales (strengths,
weaknesses, risks, missing skills, recommended actions) that explain a
fit score already computed deterministically. Never computes the score
itself and never decides application/database state.
"""
