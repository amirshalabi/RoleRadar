"""
Job normalization.

Will normalize raw job postings into a consistent schema (company,
title, location, description, URL) so they can be hashed, deduplicated,
and stored consistently regardless of source.
"""
